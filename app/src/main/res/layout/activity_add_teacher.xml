<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@drawable/gradient_background">

    <!-- Title -->
    <TextView
        android:id="@+id/add_teach"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add New Teacher"
        android:textSize="28sp"
        android:textColor="#FFFFFF"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="24dp"
        android:textStyle="bold"/>

    <!-- Avatar Selection -->
    <ImageView
        android:id="@+id/teacherAvatar"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/add_teach"
        android:layout_marginBottom="16dp"
        android:src="@color/black"
        android:background="@drawable/circle_background"
        android:scaleType="centerCrop"
        android:elevation="8dp"
        android:onClick="onChooseAvatarClick"/>

    <!-- Teacher Name -->
    <EditText
        android:id="@+id/teacherNameEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Teacher Name"
        android:layout_below="@id/teacherAvatar"
        android:layout_marginTop="16dp"
        android:inputType="textPersonName"
        android:padding="12dp"
        android:background="@drawable/edittext_background"
        android:textColorHint="#B0BEC5"
        android:textColor="#FFFFFF" />

    <!-- Teacher Phone -->
    <EditText
        android:id="@+id/teacherPhoneEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Phone Number"
        android:layout_below="@id/teacherNameEditText"
        android:layout_marginTop="16dp"
        android:inputType="phone"
        android:padding="12dp"
        android:background="@drawable/edittext_background"
        android:textColorHint="#B0BEC5"
        android:textColor="#FFFFFF" />

    <!-- Save Button -->
    <Button
        android:id="@+id/saveTeacherButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="Save Teacher"
        android:textColor="#FFFFFF"
        android:backgroundTint="#1976D2"
        android:layout_below="@id/teacherPhoneEditText"
        android:layout_marginTop="24dp"
        android:padding="12dp"
        android:elevation="4dp"/>
</RelativeLayout>
